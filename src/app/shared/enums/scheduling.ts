export enum SchedulingStatus {
    SCHEDULED = "SCHEDULED",
    CONFIRMED = "CONFIRMED",
    CANCELED = "CANCELED",
    FINISHED = "FINISHED",
}

export enum SchedulingStatusDesc {
    SCHEDULED = "Agendado",
    CONFIRMED = "Confirmado",
    CANCELED = "Cancelado",
    FINISHED = "Finalizado",
}

export enum ResponseScheduling {
    DEFAULT = "DEFAULT",
    REFRESH = "REFRESH"
}